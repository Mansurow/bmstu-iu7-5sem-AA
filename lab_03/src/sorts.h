#ifndef SORTS_H
#define SORTS_H

#include <iostream>

void print_array(int *array, size_t n);
void shell_sort(int *, size_t);
void heap_sort(int *, size_t);
void bead_sort(int *, size_t);

#endif //SORTS_H
